# Dungeon Drifter 项目复盘

## 1. 项目概述

Dungeon Drifter 是一个使用 Unity 6、C# 和 URP 制作的俯视角动作 Roguelite Demo。项目围绕“移动探索—遭遇敌人—战斗—获得掉落或升级—继续推进”的短循环展开，当前版本已经具备一轮游戏所需的主要模块。

- 引擎版本：Unity 6000.3.10f1
- 主要技术：C#、Universal Render Pipeline、Input System、AI Navigation、UGUI / TextMesh Pro
- 操作方式：WASD 移动，空格攻击
- 复盘范围：仓库截至 `c0c6c2c`（2026-08-03）的实现

## 2. 做了哪些系统

| 系统 | 主要内容 | 关键实现 |
| --- | --- | --- |
| 玩家移动与镜头 | 基于新 Input System 读取二维移动输入，使用 Rigidbody 移动；镜头平滑跟随玩家 | `PlayerController`、`CameraFollow` |
| 数据驱动武器与战斗 | 剑、弓、锤等武器使用 ScriptableObject 配置伤害、范围和冷却；攻击时进行范围检测并避免同一次攻击重复伤害同一目标 | `WeaponData`、`WeaponAttack` |
| 敌人 AI | 敌人在巡逻、追击、攻击三种状态之间切换，使用 NavMeshAgent 寻路并按冷却攻击玩家 | `EnemyAI` |
| 敌人类型配置 | Scout、Raider、Soldier、Brute、Elite 使用独立数据资产配置生命、攻击、移速等属性，并配有不同预制体和材质 | `EnemyData`、`Assets/Data/Enemies` |
| 生命与死亡 | 玩家和敌人共用生命组件；支持受伤、治疗、死亡事件，敌人死亡后销毁，玩家死亡后显示结算界面并可重新开始 | `Health`、`HealthBar`、`DeathScreen`、`Billboard` |
| 程序化房间 | 随机选择房间预制体，通过入口/出口对齐生成连续地图；生成完成后重新构建整张地图的 NavMesh | `Room`、`RoomGenerator` |
| 敌人生成与难度推进 | 每个房间随机生成敌人，越后面的房间敌人类型越强；最终房间固定包含 Elite，并按规则搭配 Brute 或 Soldier | `RoomGenerator` |
| 三选一升级 | 每击败 3 个敌人，从升级池中随机提供 3 个不重复选项；可提升伤害、移速、攻击范围或缩短冷却 | `UpgradeManager`、`UpgradeSelectionUI`、`UpgradeCardUI`、`PlayerStats`、`UpgradeData` |
| 掉落与拾取 | 敌人死亡后按权重掉落金币、治疗或武器；玩家接触物品后立即结算效果 | `DropTable`、`EnemyDrop`、`PickupItem`、`PlayerInventory` |
| 对象池 | 为不同掉落物分别维护对象池，避免战斗过程中频繁实例化和销毁拾取物 | `ObjectPool`、`DropPoolManager` |
| 战斗反馈 | 命中时播放粒子和屏幕震动；玩家攻击、受伤、治疗、死亡和拾取均有音效 | `EnemyHitFeedback`、`ScreenShake`、`PlayerSound` |
| 敌人音效 | 监听敌人的攻击、受伤和死亡事件；死亡时将音频发射器与即将销毁的敌人分离，保证死亡音效可以播完 | `EnemySound`、`EnemyAudioEmitter` |
| 编辑器自动配置 | 自动创建升级、掉落数据与 UI，并把所需组件配置到玩家和敌人预制体中，减少手工接线 | `UpgradeSystemSetup`、`DropSystemSetup` |

## 3. 最大难点

### 随机房间、NavMesh 与敌人生成的联动

最难的部分不是单独生成一个房间，而是保证随机拼接后的整张地图仍然能让玩家、敌人和寻路系统正确工作。它同时涉及房间坐标、连接方向、NavMesh 构建时机、敌人出生点合法性和难度推进，任一步骤的顺序或坐标计算出错，都可能出现房间重叠、入口错位、敌人生成在墙内，或 NavMeshAgent 不在导航网格上的问题。

具体挑战包括：

1. 每个房间的入口和出口处于各自的局部坐标，需要同时计算旋转差与位置差，不能只做平移。
2. 房间预制体中的本地 NavMesh 数据不能直接代表随机拼接后的最终地图。
3. 敌人必须在最终 NavMesh 构建完成后生成，否则出生位置和寻路状态不可靠。
4. 随机地图还要维持可控的难度曲线，而不是完全随机地堆放敌人。

## 4. 怎么解决的

项目采用了分阶段的生成流程：

1. **抽象房间连接点**：每个 `Room` 明确提供 `Entrance` 和 `Exit`，生成器不依赖房间内部具体结构。
2. **同时对齐旋转和位置**：新房间先根据前一房间出口计算旋转偏移，再补偿入口到出口的位置差，使连接关系保持一致。
3. **统一构建导航网格**：实例化房间后禁用各房间的局部 NavMeshSurface，等所有房间就位后，由生成器在父节点上重新构建一张完整 NavMesh。
4. **最后生成敌人**：地图和 NavMesh 完成后才执行敌人生成，并使用 `NavMesh.SamplePosition` 将候选点校正到可行走区域。
5. **按房间索引控制敌人池**：前期以 Scout、Raider 为主，中后期逐步加入 Soldier、Brute，最终房间加入 Elite，在保留随机性的同时维持难度递进。
6. **通过数据资产降低耦合**：敌人数值放在 `EnemyData` 中，生成规则只决定“生成哪一种敌人”，无需了解具体生命、伤害和速度。

这一方案把复杂问题拆成了“布局—导航—生成”三个阶段，明确了依赖顺序，也让房间内容和敌人数值可以独立调整。

## 5. 其他问题与处理

### 系统之间容易互相引用

击杀会同时影响掉落和升级，攻击与生命变化又会触发 UI、声音和粒子。如果所有逻辑都直接互相调用，脚本很快会难以维护。当前实现通过 `OnDeath`、`OnDamaged`、`OnHealed`、`OnAttack`、`OnItemCollected` 等事件分发状态变化，让音效、掉落和 UI 作为监听者接入。

### 死亡对象销毁会截断音效

敌人死亡后会立即销毁，但挂在敌人身上的 AudioSource 也会随之消失。解决方法是把 `EnemyAudioEmitter` 临时移出敌人层级，按音频长度等待播放完成后再销毁。

### 高频掉落可能产生运行时开销

金币、治疗和武器拾取会反复生成。项目按预制体维护独立对象池，取出时初始化掉落数据，拾取成功后归还，从而减少重复创建和销毁。

### Unity 资源配置步骤较多

升级卡片、掉落表、材质、拾取物预制体及组件引用需要大量手工配置。项目增加了编辑器脚本，用固定规则创建资源并写入预制体，降低遗漏引用和配置不一致的概率。

## 6. 如果重做会怎么改

1. **先定义游戏流程状态机**：建立明确的 `Generating`、`Playing`、`ChoosingUpgrade`、`GameOver` 状态，统一管理暂停、输入和界面，避免多个 UI 脚本分别修改 `Time.timeScale`。
2. **拆分 RoomGenerator 的职责**：将地图布局、NavMesh 构建和敌人生成拆成三个服务，使每部分都能单独测试和替换。
3. **加入随机种子和生成记录**：允许输入固定种子复现同一地图，并记录房间序列、连接信息和出生点，方便定位偶发生成问题。
4. **使用连接规则验证房间**：为入口/出口增加类型、朝向和占用范围，生成时检查包围盒重叠，避免房间在复杂布局下相交。
5. **统一依赖注入方式**：减少运行时 `FindFirstObjectByType`、全局单例和硬编码资源路径，改为由场景入口统一装配依赖，或通过清晰的接口传入。
6. **扩大对象池范围**：除了掉落物，也对常用敌人、命中特效和房间进行池化；当前掉落池已经证明这种模式适合项目。
7. **统一输入方案**：移动已经使用 Input System，而攻击仍读取 `Input.GetKeyDown`；重做时会全部改为 Input Actions，方便改键、手柄和 UI 输入管理。
8. **让编辑器工具可验证、可重复执行**：去除对固定预制体路径的依赖，执行前检查资源与字段，执行后输出验证结果，避免资源移动后静默失效。
9. **补充自动化测试**：至少覆盖加权掉落、三选一不重复、数值升级叠乘、生命事件只触发一次、固定种子地图可连通，以及对象池取还流程。
10. **加强玩家可见信息**：加入金币数、当前武器、累计升级、剩余升级击杀数和房间进度，让系统状态不只存在于日志中。

## 7. 总结

这个项目已经从基础移动和攻击逐步扩展为一个系统较完整的 Roguelite 原型。最有价值的部分是把程序化房间、运行时导航、敌人难度、掉落和升级串成了可玩的闭环，并开始使用 ScriptableObject、事件和对象池控制系统复杂度。

下一阶段若继续开发，优先级应是提高生成稳定性与可复现性、统一游戏流程和输入、补充测试与状态 UI，再扩展更多房间和内容。这样可以先把底层循环做稳，再以较低成本增加武器、敌人、升级和关卡变化。
